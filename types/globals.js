// NOTE ensure not sharing the same name as a typescript file in the directory
// or this file will be shadowed

/**
 * Global types that have a source implementation.
 */

/**
 * @import { JSONResponse } from "../utils/fetch.js";
 * @import { Component } from "../components/component.js"
 * @import { ComponentProps } from "../utils/fe.js"
 * @import { Express,
 *  Request as ExpressRequest,
 *  RequestHandler as ExpressRequestHandler,
 *  Response as ExpressResponse,
 *  NextFunction } from "express"
 */

// rollup var substituted at build time
/** @type {boolean} */
var __DEV__;

/** @type {any} */
var __cssReady;
